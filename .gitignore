# Saved Google Drive pages (contain Google's own embedded API keys → false secret alerts)
*Google Drive*.html
*- Google Drive.html

# OS junk
.DS_Store
Thumbs.db

# Claude Code session artifacts — handoffs, auto-wrap snapshots, settings.
# This is a public GitHub Pages repo deployed via `git add .`; .claude/
# must never be staged into it.
.claude/
